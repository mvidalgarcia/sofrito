{
  "$schema": "https://opencode.ai/config.json",
  "command": {
    "check": {
      "description": "Run typecheck and lint",
      "template": "Run typecheck, lint, and formatting check: `pnpm run typecheck && pnpm run lint && pnpm run format:check`\nFix any issues found."
    },
    "push": {
      "description": "Stage, review, check, commit, and push",
      "template": "Stage all changes, review the diff, run checks, then commit and push by running `bash run/push.sh`."
    }
  }
}
